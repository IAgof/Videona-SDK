v0.6.2
- Fixed size text in video, same in preview and video exported.
- Fixed initialization of TranscoderHelper, needed TextToDrawable object.
- TranscoderHelper. Added return ListenableFuture to adaptVideoWithRotationToDefaultFormatAsync.

